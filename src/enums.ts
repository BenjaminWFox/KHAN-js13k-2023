export enum DeckCollections {
  HAND = 'HAND',
  DRAW = 'DRAW',
  DONE = 'DONE',
}

export enum SPRITE_TYPE {
  player = 'player',
  enemy = 'enemy',
  mount = 'mount',
}

export enum ENEMY_INTENT {
  assault = 'assault',
  defence = 'defend',
  ability = 'ability',
}
